[project]
name = "nemotron-reasoning-starter"
version = "0.1.0"
description = "Clean starter repo for NVIDIA Nemotron Model Reasoning Challenge"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
